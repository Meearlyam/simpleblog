#hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
    url: 'jdbc:postgresql://localhost:5433/blogdb?useUnicode=true&characterEncoding=utf8;DB_CLOSE_DELAY=-1'
    username: 'postgres'
    password: 'JIT..waveMode'
server:
  port: 8080
  servlet:
    context-path: '/simpleblog'